package io.sketch.dsql.core.node;

public enum NodeType {
    TEXT,
    IF,
    FOR_EACH,
    WHERE,
    SET,
    CHOOSE,
    WHEN,
    OTHERWISE,
    TRIM,
    BIND,
    MIXED,
    WITH,
    UNION,
    JOIN
}